{
  "name": "ai-harness",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "npm install",
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "dev": "tsx packages/harness-cli/src/index.ts",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
