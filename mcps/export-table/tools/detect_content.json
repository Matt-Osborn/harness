{
  "name": "detect_content",
  "description": "Detect whether content is a table or mermaid diagram and which parser matched (gfm, html, ascii, delimited, mermaid).",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000000,
        "description": "Raw content to classify."
      }
    },
    "required": [
      "content"
    ]
  }
}