# purge

Remove empty sessions that have no messages.

| Invocation | Behavior |
|---|---|
| `--purge-empty-sessions` | Remove all empty sessions |
| `--purge-empty-sessions --dry-run` | List which sessions would be removed without deleting |

Use `--dry-run` first to preview, then run without it to purge.