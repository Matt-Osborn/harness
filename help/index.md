# help

Topic-based help for harness-cli flags and commands.

| Topic | Description |
|-------|-------------|
| `width` | `--width / -w` — output line wrapping |
| `cancel` | Ctrl+C / double-Esc — cancel in-progress requests |
| `search` | `--search / -s` — search provider selection |
| `session` | `--session / -S`, `--resume / -r` — session management |
| `agent` | `--agent` — agent selection |
| `prompt` | `-p / --prompt` — print mode |
| `purge` | `--purge-empty-sessions`, `--dry-run` |
| `mode` | `--plan`, `--build` — operating modes |
| `export` | `/export` — session export |
| `routing` | `--routing` — OpenRouter provider routing |

Usage: `harness help <topic>` or `harness --help <topic>`